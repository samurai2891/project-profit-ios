# リリース残課題チェックリスト

更新日: 2026-03-24  
対象 HEAD: `f2b06320e11e2bbb6a7c8e1b1740c613d6de9aa3`
注記: `REL-RM-01` の完了判定は current working tree 基準で実施。

本書は、current HEAD を release final 判定まで進めるために残っている修正項目だけを管理するチェックリストです。  
既に修正済みの build blocker、backup/restore、tax pack 整合、year lock 基本防御、share import 診断性の改善は本書の対象外とし、未完了の release blocker と release evidence 再採取だけを扱います。

## 判定

- 現時点の判定は `no-go`
- `P0` 3件と `P1` 1件、および current HEAD での release evidence 再採取が完了するまで release final には進めない

## 全体チェック

- [x] `REL-RM-01` 検索・証跡インデックス破損を明示障害化する
- [ ] `REL-RM-02` 年度締め preflight を UseCase 強制にする
- [ ] `REL-RM-03` 配賦承認リクエストに `pending` 制約を入れる
- [ ] `REL-RM-04` 承認キュー UI でもロック年度の保存を止める
- [ ] `REL-RM-05` current HEAD で release evidence を採り直す
- [ ] current HEAD に対する go / no-go 判定を更新する

## REL-RM-01 検索・証跡インデックス破損の明示障害化

優先度: `P0`  
担当: `15 search/OCR/tax-pack infra`

### 対象ファイル

- `ProjectProfit/Application/Mappers/CanonicalJSONCoder.swift#L33`
- `ProjectProfit/Infrastructure/Search/LocalJournalSearchIndex.swift#L125`
- `ProjectProfit/Infrastructure/Search/LocalJournalSearchIndex.swift#L131`
- `ProjectProfit/Infrastructure/Search/LocalJournalSearchIndex.swift#L137`
- `ProjectProfit/Infrastructure/Search/LocalJournalSearchIndex.swift#L142`
- `ProjectProfit/Infrastructure/Search/LocalEvidenceSearchIndex.swift#L139`
- `ProjectProfit/Infrastructure/Persistence/SwiftData/Repositories/SwiftDataEvidenceRepository.swift#L36`
- `ProjectProfit/Infrastructure/Persistence/SwiftData/Repositories/SwiftDataEvidenceRepository.swift#L123`

### 修正チェックリスト

- [x] `CanonicalJSONCoder.decode` の decode failure を `fallback` で吸収する経路を見直し、破損を明示エラーまたは診断状態として表現する
- [x] `LocalJournalSearchIndex` で `counterpartyNamesJSON`、`registrationNumbersJSON`、`projectIdsJSON`、`fileHashesJSON` の decode failure を空配列扱いしない
- [x] `LocalEvidenceSearchIndex` で `projectIdsJSON` の decode failure を空配列扱いしない
- [x] `SwiftDataEvidenceRepository.search` が「索引破損」と「検索結果ゼロ」を区別できるようにする
- [x] `autoRepairSearchIndexIfNeeded` の `indexCount == 0` 前提を見直し、部分破損でも検出・再構築できるようにする
- [x] 再索引失敗時に処理を継続せず、呼び出し元へ failure を返す

### テストチェックリスト

- [x] [SearchIndexTests.swift](../../ProjectProfitTests/SearchIndexTests.swift) に journal index JSON 破損ケースを追加する
- [x] [SearchIndexTests.swift](../../ProjectProfitTests/SearchIndexTests.swift) に evidence index JSON 破損ケースを追加する
- [x] [CanonicalRepositoriesTests.swift](../../ProjectProfitTests/CanonicalRepositoriesTests.swift) か新規テストで partial corruption 検出ケースを追加する
- [x] 再索引 failure が空結果ではなく error になることをテストで固定する

### 確認結果

- `SearchIndexTests` 6件成功
- `CanonicalRepositoriesTests` 8件成功
- 実行コマンド: `xcodebuild -scheme ProjectProfit -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:ProjectProfitTests/SearchIndexTests -only-testing:ProjectProfitTests/CanonicalRepositoriesTests test`

### 完了条件

- [x] 壊れた索引データで検索しても `[]` ではなく failure として識別できる
- [x] 部分破損でも検出できる
- [x] 正常系の検索結果順序と既存検索仕様が維持される

## REL-RM-02 年度締め preflight の UseCase 強制

優先度: `P0`  
担当: `9 filing/closing/reporting`

### 対象ファイル

- `ProjectProfit/Application/UseCases/Closing/ClosingWorkflowUseCase.swift#L74`
- `ProjectProfit/Application/UseCases/Filing/FilingPreflightUseCase.swift#L117`
- `ProjectProfit/Application/UseCases/Filing/FilingPreflightUseCase.swift#L153`

### 修正チェックリスト

- [ ] `ClosingWorkflowUseCase.transitionFiscalYearState` の中で preflight を必須実行する
- [ ] pending candidate、closing entry 不足、申告前提不足を state transition 前に必ず検出する
- [ ] UI から呼ばれない経路でも同じ制約が効くようにする
- [ ] preflight failure を年度締め文脈で理解できるエラーへ変換する

### テストチェックリスト

- [ ] [ClosingWorkflowUseCaseTests.swift](../../ProjectProfitTests/ClosingWorkflowUseCaseTests.swift) に pending candidate 残存時の失敗ケースを追加する
- [ ] [ClosingWorkflowUseCaseTests.swift](../../ProjectProfitTests/ClosingWorkflowUseCaseTests.swift) に closing entry 不足時の失敗ケースを追加する
- [ ] [ClosingWorkflowUseCaseTests.swift](../../ProjectProfitTests/ClosingWorkflowUseCaseTests.swift) に正常遷移成功ケースを追加する
- [ ] [FilingPreflightUseCaseTests.swift](../../ProjectProfitTests/FilingPreflightUseCaseTests.swift) と整合する error 内容を確認する

### 完了条件

- [ ] UI を経由しなくても不正な年度締め遷移が止まる
- [ ] preflight failure 理由がそのまま release blocker として追跡できる

## REL-RM-03 配賦承認リクエストの `pending` 制約追加

優先度: `P0`  
担当: `10 posting/withholding`

### 対象ファイル

- `ProjectProfit/Application/UseCases/Posting/ApprovalQueueWorkflowUseCase.swift#L168`
- `ProjectProfit/Application/UseCases/Posting/ApprovalQueueWorkflowUseCase.swift#L184`
- `ProjectProfit/Application/UseCases/Posting/ApprovalQueueWorkflowUseCase.swift#L203`
- `ProjectProfit/Application/UseCases/Posting/ApprovalQueueWorkflowUseCase.swift#L224`

### 修正チェックリスト

- [ ] distribution request の approve 前に `status == .pending` を必須化する
- [ ] distribution request の reject 前に `status == .pending` を必須化する
- [ ] distribution request の invalidate 前に `status == .pending` を必須化する
- [ ] 二重承認、承認後却下、却下後無効化のような不正遷移を明示エラーにする
- [ ] recurring request と distribution request の状態遷移ルール差分を整理し、必要なら同等の guard を入れる

### テストチェックリスト

- [ ] [ApprovalQueueWorkflowUseCaseTests.swift](../../ProjectProfitTests/ApprovalQueueWorkflowUseCaseTests.swift) に distribution request の非 pending 承認失敗ケースを追加する
- [ ] [ApprovalQueueWorkflowUseCaseTests.swift](../../ProjectProfitTests/ApprovalQueueWorkflowUseCaseTests.swift) に distribution request の非 pending 却下失敗ケースを追加する
- [ ] [ApprovalQueueWorkflowUseCaseTests.swift](../../ProjectProfitTests/ApprovalQueueWorkflowUseCaseTests.swift) に distribution request の非 pending 無効化失敗ケースを追加する
- [ ] [ApprovalQueueWorkflowUseCaseTests.swift](../../ProjectProfitTests/ApprovalQueueWorkflowUseCaseTests.swift) に pending のみ成功するケースを追加する

### 完了条件

- [ ] 配賦承認リクエストの状態機械が `pending` 起点に固定される
- [ ] 二重処理が明示的に拒否される

## REL-RM-04 承認キュー UI のロック年度保存抑止

優先度: `P1`  
担当: `17 feature UI A`

### 対象ファイル

- `ProjectProfit/Features/ApprovalQueue/ApprovalQueueView.swift#L466`
- `ProjectProfit/Features/ApprovalQueue/ApprovalQueueView.swift#L499`
- `ProjectProfit/Features/ApprovalQueue/ApprovalQueueView.swift#L786`
- `ProjectProfit/Features/ApprovalQueue/ApprovalQueueView.swift#L897`
- `ProjectProfitUITests/WithholdingApprovalUITests.swift`

### 修正チェックリスト

- [ ] 保存ボタンの `disabled` 条件に `isCandidateYearLocked` を追加する
- [ ] 保存ボタンと承認ボタンでロック年度時の表示メッセージを揃える
- [ ] ロック年度では「保存できない」ことが画面上で分かるようにする
- [ ] バックエンド側 year lock 防御と UI の見え方を一致させる

### テストチェックリスト

- [ ] [WithholdingApprovalUITests.swift](../../ProjectProfitUITests/WithholdingApprovalUITests.swift) にロック年度で保存ボタンが無効なケースを追加する
- [ ] 既存の approval tab / journal browser / settings identifier テストが崩れていないことを確認する

### 完了条件

- [ ] ロック年度では保存も承認も同じ基準で押せない
- [ ] ユーザが「押したら backend で失敗する」状態に入らない

## REL-RM-05 current HEAD での release evidence 再採取

優先度: `P0`  
担当: `1 config/CI`

### 対象ファイル

- `.github/workflows/release-quality.yml`
- `scripts/run_release_quality_lane.sh`
- `scripts/check_xcodegen_sync.sh`
- `Docs/release/checklist.md`
- `Docs/release/quality/README.md`
- `Docs/release/quality/release-build.md`
- `Docs/release/quality/golden-baseline.md`
- `Docs/release/quality/canonical-e2e.md`
- `Docs/release/quality/migration-rehearsal.md`
- `Docs/release/quality/performance-gate.md`
- `Docs/release/quality/latest-lane.md`
- `Docs/release/quality/latest.md`

### 実行チェックリスト

- [ ] `xcodegen generate` 実行後に `bash scripts/check_xcodegen_sync.sh` が `status=ok` になることを確認する
- [ ] `release-build` を current HEAD で再実行する
- [ ] `golden-baseline` を current HEAD で再実行する
- [ ] `canonical-e2e` を current HEAD で再実行する
- [ ] `migration-rehearsal` を current HEAD で再実行する
- [ ] `performance-gate` を current HEAD で再実行する
- [ ] 必要に応じて `books` と `forms` も current HEAD で再実行する

### 証跡チェックリスト

- [ ] 各 lane の Markdown に `head_sha` が current HEAD で記録されている
- [ ] 各 lane の Markdown に `run_id` と `run_url` が入っている
- [ ] `status`、`reason`、`mode`、`configuration`、`simulator_device`、`test_summary` が埋まっている
- [ ] `summary_path`、`log_path`、`xcresult_path`、`metrics_path` が有効な値になっている
- [ ] `latest.md` を更新する場合は fully-green snapshot の条件を満たしている

### 完了条件

- [ ] current HEAD の release evidence が repo 内の正本として揃う
- [ ] current HEAD の go / no-go を `Docs/release/checklist.md` と lane 個票だけで判定できる

## 最終判定チェック

- [ ] `REL-RM-01` から `REL-RM-05` まで完了している
- [ ] `P0` 項目に未完了が残っていない
- [ ] current HEAD の lane 個票で `status: error` が残っていない
- [ ] release final の go / no-go を更新した
