# project-profit-ios リリース修正タスク統合版（P0 / P1）

更新日: 2026-03-15  
対象リポジトリ: `project-profit-ios`  
対象 ZIP: `project-profit-ios.zip` / `project-profit-ios-local.zip`  
統合元 Markdown:
- `修正タスク一覧_P0_P1_必要書類作成のみ.md`
- `白色申告_詳細修正タスク一覧_P0_P1.md`

本書は、上記 2 本の Markdown の内容を **重複を整理して 1 本へ統合**したものです。  
また、統合にあたり **ZIP を再読込して確認できた事実だけ**を補足しています。

---

## 0. 前提

- **提出機能は実装しない**。
- ユーザ自身が提出する前提とし、アプリの責務は **必要書類データの正しい作成まで** とする。
- 本書は、現行実装、同梱 official XSD / 帳票フィールド仕様書、既存 2 本の Markdown に記載された確認結果を統合したものとする。
- 推測は含めず、確認できた事実だけを記載する。

### 0-1. 今回のスコープ内
- 青色申告決算書（一般）データ作成
- 白色申告（収支内訳書）データ作成
- 青色申告決算書（現金主義用）データ作成
- preview / export / XSD 整合
- 2025 / 2026 filing pack 整合
- 関連する共通宣言部（declarant / 年分 / requiredRule / validation）
- release 用 quality / test / docs 整備

### 0-2. 今回のスコープ外
- 利用者識別番号ログイン
- 電子署名
- 送受信モジュール
- 受信通知 / メッセージボックス
- 直接提出機能
- API 送信連携

---

## 1. ZIP 再確認で確認した事実

### 1-1. 対象 checkout
- `project-profit-ios.zip` を再展開して確認した current HEAD は次。  
  `bacc2dcd42c3d9f51a1da87d8832d760ce3cefd6`

### 1-2. 対象ファイルの存在確認
ZIP 再読込で、少なくとも以下の実装・参照ファイルの存在を確認した。

- `ProjectProfit/Services/EtaxXtxExporter.swift`
- `ProjectProfit/Services/ShushiNaiyakushoBuilder.swift`
- `ProjectProfit/Features/Filing/Presentation/Screens/FilingDashboardView.swift`
- `ProjectProfitTests/EtaxXtxExporterTests.swift`
- `ProjectProfit/Resources/TaxYearPacks/2025/filing/blue_cash_basis.json`
- `ProjectProfit/Resources/TaxYearPacks/2025/filing/blue_general.json`
- `ProjectProfit/Resources/TaxYearPacks/2025/filing/common.json`
- `ProjectProfit/Resources/TaxYearPacks/2025/filing/white_shushi.json`
- `project-profit-ios-local/e-taxall/19XMLスキーマ/shotoku/KOA020-018.xsd`
- `project-profit-ios-local/e-taxall/19XMLスキーマ/shotoku/KOA110-012.xsd`
- `project-profit-ios-local/e-taxall/19XMLスキーマ/shotoku/KOA210-011.xsd`
- `project-profit-ios-local/e-taxall/19XMLスキーマ/shotoku/KOA220-008.xsd`
- `project-profit-ios-local/e-taxall/19XMLスキーマ/shotoku/KOA230-010.xsd`

### 1-3. ZIP 再確認で更新した数値事実
- `white_shushi.json` の field 数は 2025 / 2026 とも **17**。
- 同梱 `帳票フィールド仕様書(所得-申告)Ver12x.xlsx` の `KOA110` シートは、再集計で **159 タグ / 145 ユニークタグ**。
- `2025/filing/white_shushi.json` と `2026/filing/white_shushi.json` は、`taxYear` と `filingDeadline` を除けば同一。

> 注記: 既存の白色詳細 Markdown には `158 タグ / 144 ユニークタグ` とあるが、本統合版では **ZIP 再読込時の再集計値（159 / 145）** を採用する。

---

## 2. 既存 2 本の Markdown を統合した確認事項

### 2-1. 全体側で既に確認済みの論点
- 2025年分 filing pack の `filingDeadline` が 4 ファイルとも `2026-03-17` になっている。
- 現金主義フォームは `UI=blueCashBasis / pack=KOA220 / exporter=KOA210 / official current XSD=KOA230` で不一致がある。
- 現金主義の主要 3 項目は `xmlTag: null`。
- 青色一般 exporter は `KOA210-1` しか出していない。
- 白色 exporter は `KOA110-1` しか出していない。
- `ABA...` 系の declarant 情報を `KOA210 / KOA110` に混在させている。
- 一部の field → xmlTag に誤マッピングがある。
- 一部で複合要素タグへ直接値を入れる設計になっている。
- `builder -> pack -> export` の coverage が不足している。
- current XSD 検証テストは 3 フォーム全部を十分にカバーしていない。
- release quality 証跡が current HEAD と同期していない。

### 2-2. 白色詳細側で既に確認済みの論点
- `ShushiNaiyakushoBuilder` は「収入 — 売上のみ（白色は簡易）」と明記している。
- `white_shushi.json` は 17 field と小さい。
- current exporter は `AIG` と `AIN` を浅い固定階層に押し込んでいる。
- `shushi_revenue_total -> AIG00020` は direct value 想定だが、official では複合要素。
- `shushi_rent_breakdown -> AIN00090` は fieldLabel が「地代家賃合計」だが、official `AIN00090` の意味と一致しない。
- `KOA110-2` にある `AIK / AIL / AIM / AIN` が current white pack に揃っていない。
- `white_shushi.json` に `requiredRule` が無い。
- white XSD 検証は generated XML が無い場合に fixture fallback できる。

---

# 3. P0（書類作成の正しさを阻害する項目）

## P0-01 2025年分の申告期限を修正する

### 内容
- 2025年分 filing pack の `filingDeadline` を `2026-03-16` に修正する。
- 対象は `common / blue_general / white_shushi / blue_cash_basis` の 4 ファイル。

### 対象ファイル
- `ProjectProfit/Resources/TaxYearPacks/2025/filing/common.json`
- `ProjectProfit/Resources/TaxYearPacks/2025/filing/blue_general.json`
- `ProjectProfit/Resources/TaxYearPacks/2025/filing/white_shushi.json`
- `ProjectProfit/Resources/TaxYearPacks/2025/filing/blue_cash_basis.json`

### 根拠
- current 4 ファイルとも `2026-03-17` を保持している。
- 既存 Markdown はこれを問題として扱っている。
- 白色詳細 Markdown でも `white_shushi.json` 2025 の deadline ずれを明示している。

### 作業
1. 上記 4 ファイルの `filingDeadline` を `2026-03-16` に修正する。
2. UI 上で deadline を参照する箇所があれば、pack 値を参照することを確認する。
3. deadline を検証する pack lint / unit test を追加する。

### 白色詳細での追加粒度
- white 単独ではなく、**2025 filing pack 全体を同時に修正**する。
- white 側の regression test にも deadline を含める。

### 完了条件
- 2025年分 filing pack の deadline が全件 `2026-03-16`。
- deadline ずれを CI で検出できる。

---

## P0-02 現金主義フォームの帳票 ID / rootTag / version を official current 仕様に揃える

### 内容
- `blueCashBasis` の帳票系統を official current 仕様に合わせて再定義する。
- `UI=blueCashBasis / pack=KOA220 / exporter=KOA210 / official current XSD=KOA230` の不一致を解消する。
- `EtaxFormType`・pack 定義・exporter の参照先を 1 系統に統一する。

### 対象ファイル
- `ProjectProfit/Models/EtaxModels.swift`
- `ProjectProfit/Resources/TaxYearPacks/2025/filing/blue_cash_basis.json`
- `ProjectProfit/Resources/TaxYearPacks/2026/filing/blue_cash_basis.json`
- `ProjectProfit/Services/EtaxXtxExporter.swift`
- 必要なら `ProjectProfit/Services/TaxYearDefinitionLoader.swift`

### 根拠
- UI は `blueCashBasis` を選択可能。
- exporter は `.blueCashBasis` を `.blueReturn` と同じ `buildBlueReturnXml` に流し、`blue_general` の `KOA210 / 11.0` を使う。
- `blue_cash_basis.json` は `KOA220 / 7.0` を宣言している。
- 既存 Markdown では official current XSD 側の現金主義帳票が `KOA230` と整理されている。

### 作業
1. 現金主義の official current 帳票 ID / rootTag / version を 1 系統に確定する。
2. `EtaxFormType` と pack の form ID / formVer / rootTag を揃える。
3. exporter が `blue_general` metadata を参照しないようにする。
4. `TaxYearDefinitionLoader` の form metadata 読み出し先を見直す。

### 完了条件
- `blueCashBasis` の `rootTag / VR / page tag` が official current 仕様と一致する。
- pack / model / exporter の form ID が 1 系統に統一される。

---

## P0-03 現金主義の主要金額を export 対象に残るようにする

### 内容
- `cash_basis_revenue / cash_basis_expense_total / cash_basis_income` に official xmlTag を付与する。
- `CashBasisReturnBuilder` が生成する `cash_basis_expense_*` を pack または exporter 側で正式に扱う。
- `exportableForm` による silent drop を解消する。

### 対象ファイル
- `ProjectProfit/Resources/TaxYearPacks/2025/filing/blue_cash_basis.json`
- `ProjectProfit/Resources/TaxYearPacks/2026/filing/blue_cash_basis.json`
- `ProjectProfit/Services/CashBasisReturnBuilder.swift`
- `ProjectProfit/ViewModels/EtaxExportViewModel.swift`
- 必要なら `ProjectProfit/Services/EtaxCharacterValidator.swift`

### 根拠
- 主要 3 項目が `xmlTag: null`。
- `CashBasisReturnBuilder` は `cash_basis_expense_1..n` を動的生成するが、pack に定義が無い。
- `exportableForm` は `xmlTag` がある field のみ残す。

### 作業
1. 主要 3 項目へ official xmlTag を付与する。
2. `cash_basis_expense_*` の扱いを決める。
   - pack へ正式 field 定義を追加する、または
   - exporter 側で row 展開ロジックを持つ。
3. preview / export で同じ field セットになることを確認する。

### 完了条件
- 現金主義の preview / export の両方で主要 3 項目が保持される。
- 現金主義の経費行が XML に反映される。
- `missingXmlTag` で落ちない。

---

## P0-04 現金主義の exporter を青色一般から分離する

### 内容
- `EtaxXtxExporter` に現金主義専用の build ルートを追加する。
- `.blueCashBasis` を `buildBlueReturnXml` から分離する。

### 対象ファイル
- `ProjectProfit/Services/EtaxXtxExporter.swift`
- 必要なら `ProjectProfit/Models/EtaxModels.swift`

### 根拠
- 現在は `.blueCashBasis` が `.blueReturn` と同じ経路に流れている。
- 青色一般の grouping (`AMF/AMG/...`) を現金主義にも適用している。

### 作業
1. `buildBlueCashBasisXml(...)` 相当の専用経路を追加する。
2. 現金主義用の page / prefix / grouping を official current 帳票に合わせる。
3. `.blueCashBasis` が青色一般の `KOA210` 構造を再利用しないようにする。

### 完了条件
- `.blueCashBasis` が専用ビルダー経路を通る。
- 現金主義出力が青色一般の `KOA210` 構造を再利用しない。

---

## P0-05 青色一般 exporter を official XSD のページ構造へ作り直す

### 内容
- `KOA210-1` だけを出す構造をやめ、official `KOA210-1` 〜 `KOA210-4` に分割して出力する。
- `AMG00000`（貸借対照表）を `KOA210-4` 側へ移す。
- `AMF` 系の子要素を official 親階層に従って配置する。

### 対象ファイル
- `ProjectProfit/Services/EtaxXtxExporter.swift`
- `ProjectProfit/Resources/TaxYearPacks/2025/filing/blue_general.json`
- `ProjectProfit/Resources/TaxYearPacks/2026/filing/blue_general.json`

### 根拠
- current exporter は常に `<KOA210-1>` しか出していない。
- official `KOA210` は 4 ページ構成。
- 貸借対照表は official では `KOA210-4` 側。

### 作業
1. `KOA210-1` / `KOA210-2` / `KOA210-3` / `KOA210-4` を別構造で組み立てる。
2. `AMG00000` を page 4 側へ移す。
3. `AMF` の child path を official 親階層へ合わせる。
4. current の prefix 一括押し込み実装を除去する。

### 完了条件
- `KOA210` 出力が `KOA210-1..4` を必要に応じて生成する。
- representative XML が official `KOA210-011.xsd` に通る。

---

## P0-06 白色 exporter を official XSD のページ構造へ作り直す

### 内容
- `KOA110-1` だけを出す構造をやめ、official `KOA110-1` / `KOA110-2` に分割して出力する。
- `AIG` / `AIN` 配下を official 親階層に従って再構築する。
- `AIN00000`（地代家賃内訳）を `KOA110-2` 側へ移す。

### 対象ファイル
- `ProjectProfit/Services/EtaxXtxExporter.swift`
- `ProjectProfit/Resources/TaxYearPacks/2025/filing/white_shushi.json`
- `ProjectProfit/Resources/TaxYearPacks/2026/filing/white_shushi.json`
- 必要なら `TaxFieldDefinition` / `EtaxForm` 補助構造

### 根拠
- official `KOA110` は 2 ページ構成。
- current exporter は `KOA110-1` しか出力しない。
- `AIN00090` は official では `KOA110-2 > AIN00000` 配下。
- white 詳細 Markdown では page 分割を最優先にすべきとされている。

### 作業
1. `buildWhiteReturnXml(...)` を page-aware に分割する。
2. `KOA110-1` と `KOA110-2` を別配列で組み立てる。
3. page 2 に出すべき `AIK / AIL / AIM / AIN` を page 1 から切り離す。
4. `AIG` を `AIG00000` 直下へベタ置きする実装を廃止する。
5. `AIN00000` を page 2 の繰返し構造として扱う。

### 白色詳細の手戻り防止メモ
- page 分割前に tag 修正だけ先にやると再作業になる。  
  **必ず page 分割を先に実施する。**

### 完了条件
- 白色生成 XML に `KOA110-1` と `KOA110-2` が必要に応じて出る。
- representative XML が bundled official XSD に通る。

---

## P0-07 共通申告者情報の `ABA...` 流用をやめ、帳票別 official タグへ分離する

### 内容
- `common.json` の `ABA...` ベース定義を見直す。
- `KOA210` 用 declarant/year、`KOA110` 用 declarant/year、現金主義用 declarant/year を帳票別に持つ。
- `common.taxYear` 相当の年分フィールドを帳票別 official tag へ反映する。

### 対象ファイル
- `ProjectProfit/Resources/TaxYearPacks/2025/filing/common.json`
- `ProjectProfit/Resources/TaxYearPacks/2026/filing/common.json`
- `ProjectProfit/Services/EtaxXtxExporter.swift`
- `ProjectProfit/Services/EtaxFieldPopulator.swift`

### 根拠
- current `common.json` は `ABA00140 / ABA00130 / ABA00080 / ABA00090 / ABA00220` を使う。
- `ABA...` は official `KOA020` 側に存在するが、`KOA210/KOA110` 本体は別体系を使う。
- current exporter はこれを `KOA210 / KOA110` に混ぜている。
- 白色詳細 Markdown でも white 側の declarant/year を帳票別に分離する必要が挙げられている。

### 作業
1. 帳票別の declarant/year mapping を定義する。
2. white XML に `ABA...` が出ないようにする。
3. 年分 field を追加する。
4. `EtaxFieldPopulator` の declarant path を帳票別に切り替える。

### 完了条件
- 青色一般・白色・現金主義で declarant / address / date / tax year が official 帳票タグで出る。
- `ABA...` が他帳票へ混入しない。

---

## P0-08 青色一般の誤マッピングを修正する

### 内容
- 少なくとも以下を official 帳票の意味に合わせて修正する。
  - `expense_interest -> AMF00270`
  - `expense_taxes -> AMF00300`
  - `bs_total_liabilities -> AMG00760`
  - `bs_total_equity -> AMG00740`
- 2025 と 2026 の両 pack を同時に修正する。

### 対象ファイル
- `ProjectProfit/Resources/TaxYearPacks/2025/filing/blue_general.json`
- `ProjectProfit/Resources/TaxYearPacks/2026/filing/blue_general.json`

### 根拠
- official XSD 上、上記 4 タグの意味が current fieldLabel / internalKey と一致していない。

### 作業
1. 対象 4 項目の xmlTag を official 意味と一致するものへ付け替える。
2. 2025 / 2026 両 pack に同じ修正を反映する。
3. 旧誤タグを前提にしたテストが無いことを確認する。

### 完了条件
- 各 internalKey が official 帳票の意味と一致する xmlTag に付け替わる。
- 旧誤タグを前提にしたテストが残らない。

---

## P0-09 複合要素へ直接値を入れるマッピングを除去する

### 内容
- 少なくとも以下の direct mapping を廃止する。
  - `income_total_revenue -> AMF00970`
  - `inventory_cogs -> AMF00110`
  - `shushi_revenue_total -> AIG00020`
- 葉要素へ分解するか、親子関係に合わせて複数 field に置き換える。

### 対象ファイル
- `ProjectProfit/Resources/TaxYearPacks/2025/filing/blue_general.json`
- `ProjectProfit/Resources/TaxYearPacks/2026/filing/blue_general.json`
- `ProjectProfit/Resources/TaxYearPacks/2025/filing/white_shushi.json`
- `ProjectProfit/Resources/TaxYearPacks/2026/filing/white_shushi.json`
- `ProjectProfit/Services/EtaxXtxExporter.swift`

### 根拠
- `AMF00970` / `AMF00110` / `AIG00020` は official XSD では複合要素。
- current exporter は `<tag>value</tag>` で直列化する。
- white 詳細 Markdown では `AIG00020` の child 展開が必要とされている。

### 作業
1. blue / white 両方の direct mapping を洗い出す。
2. container tag を direct value に使っている field を leaf 分解する。
3. exporter も container-aware に変更する。
4. pack lint に「direct value = leaf tag only」検査を追加する。

### 完了条件
- 複合要素タグへ直接数値を入れるケースが pack から消える。
- representative XML が複合要素起因で落ちない。

---

## P0-10 白色の「簡易」実装をやめ、必要書類として不足している明細ページを追加する

### 内容
- `white_shushi` を `KOA110-1` だけの簡易版から、official `KOA110-2` を含む必要書類として拡張する。
- 少なくとも current code で未対応の `AIK / AIL / AIM / AIN` を設計対象にする。
- white 詳細 Markdown にある詳細タスクをここへ統合する。

### 対象ファイル
- `ProjectProfit/Services/ShushiNaiyakushoBuilder.swift`
- `ProjectProfit/Resources/TaxYearPacks/2025/filing/white_shushi.json`
- `ProjectProfit/Resources/TaxYearPacks/2026/filing/white_shushi.json`
- `ProjectProfit/Services/EtaxXtxExporter.swift`
- 必要なら補助 model / pack / validation files

### 根拠
- current builder は白色収入を `売上のみ` とコメントしている。
- official `KOA110-2` には `AIK / AIL / AIM / AIN` が存在する。
- white 詳細 Markdown では、以下の不足・誤用が明示されている。

### 白色詳細タスク（P0-10 内部タスク）

#### P0-10-a `AIG00020` を direct value で使うのをやめ、収入ブロックを child 要素へ展開する
- `shushi_revenue_total -> AIG00020` を廃止する。
- 少なくとも次の child field を定義する。
  - 売上（収入）金額
  - 家事消費
  - その他の収入
  - 計
- `ShushiNaiyakushoBuilder` で上記 child field を生成する。
- exporter は `AIG00020` を container として扱う。

#### P0-10-b 経費ブロックを `AIG00140 / AIG00210` の official 階層に合わせる
- `AIG00140` を container として追加する。
- `AIG00150〜AIG00200` を `AIG00140` 直下に配置する。
- `AIG00220〜AIG00330` を `AIG00210` 配下に配置する。
- `AIG00340/00360/00370/00380/00400/00405` の親位置も official に合わせる。

#### P0-10-c current pack に欠けている official 経費項目を追加する
少なくとも次を pack 設計対象にする。
- `AIG00150` 給料賃金
- `AIG00180` 貸倒金
- `AIG00230` 荷造運賃
- `AIG00300` 修繕費
- `AIG00320` 福利厚生費
- `AIG00325 / AIG00330` 追加科目
- `AIG00370 / AIG00380 / AIG00390 / AIG00405`

#### P0-10-d `AIN00090` の誤用をやめ、地代家賃内訳を official `AIN` 構造へ作り直す
- `shushi_rent_breakdown -> AIN00090` の 1 項目実装を廃止する。
- `AIN00000` の内訳行に必要な child field を定義する。少なくとも:
  - 支払先住所
  - 支払先氏名
  - 賃借物件
  - 権利金
  - 更新料
  - 賃借料
  - 必要経費算入額
- builder は total ではなく **内訳行** を生成する。
- exporter は `KOA110-2 > AIN00000` 繰返しへ出す。

#### P0-10-e `AIK`（売上明細）/ `AIL`（仕入明細）/ `AIM`（減価償却計算）を追加する
- `AIK` 売上明細 row 用 field 群を追加する。
- `AIL` 仕入明細 row 用 field 群を追加する。
- `AIM` 減価償却計算 row 用 field 群を追加する。
- 既存の `shushi_depreciation_*` を `AIM` 側へ接続する。

#### P0-10-f requiredRule を設定し、太枠必須の未入力を検出できるようにする
- official 手引きに基づいて white pack の必須項目へ `requiredRule` を付与する。
- validator の white 必須欠落テストを追加する。

#### P0-10-g 白色の declarant / 年分タグを `ABA...` から帳票別タグへ移す
- これは P0-07 と一体で実施する。
- white 側では `KOA110` official の declarant/year tag 体系へ寄せる。

#### P0-10-h white generated XML を実生成ベースで XSD 検証するように変更する
- `scripts/run_etax_unit_lane.sh` で white generated XML 必須にする。
- `tools/etax/fixtures/KOA110_minimal.xml` への fallback 依存を外すか、CI では禁止する。

### 完了条件
- 白色で official 付表・内訳ページ相当の入力/出力が可能になる。
- `KOA110-1` だけで完結する実装ではなくなる。
- `AIG00020` / `AIN00090` の誤用が無くなる。
- `AIK / AIL / AIM / AIN` を含む page 2 項目が存在する。
- white generated XML が bundled official XSD に通る。

---

## P0-11 青色の貸借対照表詳細を current builder から実際に出力できるようにする

### 内容
- `EtaxFieldPopulator.populateBalanceSheet` が生成する `bs_asset_* / bs_liability_* / bs_equity_*` を pack 定義へ追加する。
- official `KOA210-4` の構造に合わせて明細を出力できるようにする。

### 対象ファイル
- `ProjectProfit/Services/EtaxFieldPopulator.swift`
- `ProjectProfit/Resources/TaxYearPacks/2025/filing/blue_general.json`
- `ProjectProfit/Resources/TaxYearPacks/2026/filing/blue_general.json`
- `ProjectProfit/Services/EtaxXtxExporter.swift`

### 根拠
- current builder は明細キーを生成する。
- current pack は貸借対照表 totals 3 件しか持たない。
- `exportableForm` は pack 未定義 field を残さない。

### 作業
1. `bs_asset_* / bs_liability_* / bs_equity_*` の受け皿を pack に追加する。
2. official `KOA210-4` の parent path に合わせて exporter を修正する。
3. detail key が export payload に残ることを確認する。

### 完了条件
- 生成済み balance sheet detail key が export payload に残る。
- 青色の貸借対照表が total-only ではなく official 明細構造で出る。

---

## P0-12 official XSD を通す自動テストを current app 出力に追加する

### 内容
- `EtaxXtxExporterTests` へ official XSD 検証を追加する。
- 青色一般・白色・現金主義の current exporter 出力を、それぞれ official XSD に対して検証する。
- `run_etax_unit_lane.sh` の検証対象に `blue_cash_basis` を入れる。
- empty minimal fixture に依存しない実データ検証にする。

### 対象ファイル
- `ProjectProfitTests/EtaxXtxExporterTests.swift`
- `ProjectProfitTests/TaxYearDefinitionLoaderTests.swift`
- `scripts/run_etax_unit_lane.sh`
- `tools/etax/fixtures/KOA210_minimal.xml`
- `tools/etax/fixtures/KOA110_minimal.xml`

### 根拠
- current test 成功系は青色一般 / 白色のみ。
- current lane の XSD 検証対象も `blue_general` と `white_shushi` のみ。
- 現金主義の成功テストがない。
- white 側は generated XML が無い場合の fixture fallback がある。

### 作業
1. 3 フォーム全部の generated XML を使った XSD 検証を追加する。
2. `blue_cash_basis` を lane の必須検証対象へ追加する。
3. white の fixture fallback 依存を外す。
4. current production output を使った representative data test を追加する。

### 完了条件
- `blue_general / white_shushi / blue_cash_basis` の実出力が official XSD に全て通る。
- 実出力で 1 本でも落ちれば CI が fail する。

---

# 4. P1（品質向上・再発防止・運用整備）

## P1-01 2026 filing pack を 2025 の単純コピー状態から切り離す

### 内容
- 2026 の `common / blue_general / white_shushi / blue_cash_basis` は、現状 `taxYear` と `filingDeadline` 以外が 2025 と同一であるため、年分別に再検証する。
- 2026 を UI に出すなら、年分別の official source との照合結果を残す。

### 対象ファイル
- `ProjectProfit/Resources/TaxYearPacks/2026/filing/*.json`
- 必要なら `Docs/release/...`

### 根拠
- 現状の 2026 pack は実質 2025 pack の複製状態。
- ZIP 再確認で `white_shushi.json` 2025 / 2026 は `taxYear` と `filingDeadline` を除けば同一だった。

### 追加する white 詳細観点
- `white_shushi.json` 2025 / 2026 の差分は **year / deadline 以外は一致**という検査を自動化する。

### 完了条件
- 2026 各 pack について「official source 照合済み」の証跡が残る。
- 未照合なら UI に出さない、または実験扱いにしない方針が明文化される。

---

## P1-02 stale preview / stale export を防ぐ

### 内容
- export 実行時は `fiscalYear/formType` だけでなく、元データ変更も検知して preview を再生成する。
- `skipPreflightValidation: true` 前提の古い preview export を防ぐ。

### 対象ファイル
- `ProjectProfit/Views/Accounting/EtaxExportView.swift`
- `ProjectProfit/ViewModels/EtaxExportViewModel.swift`
- `ProjectProfit/Services/ExportCoordinator.swift`

### 根拠
- current 実装では `(fiscalYear, formType)` が同じなら preview 再生成されない。
- export 側では `skipPreflightValidation: true` を渡している。

### 追加する white 詳細観点
- white form で preview 表示 field と export field が一致する integration test を追加する。

### 完了条件
- preview 後に元帳簿が変わった場合、再 preview なし export ができない。
- export 前に current data に対する build/validation が必ず走る。

---

## P1-03 pack 定義の coverage 監査を追加する

### 内容
- current builder が生成し得る internalKey と、pack が定義する internalKey の coverage 差分を自動検査する。
- 少なくとも `CashBasisReturnBuilder`、`ShushiNaiyakushoBuilder`、`EtaxFieldPopulator` の生成キーが pack に存在するかを検査する。

### 対象ファイル
- 新規テスト / スクリプト
- `ProjectProfitTests/...`
- `scripts/...`

### 根拠
- current では builder 側の動的キーと pack 定義不足が release まで露出している。

### 追加する white 詳細観点
white 専用 lint として、少なくとも次を追加する。
- `AIG00020` のような container tag を direct value field に使わない。
- `AIN00090` を total label に使わない。
- `AIK / AIL / AIM / AIN` の page 2 field が存在する。
- `requiredRule` の必須項目が欠けていない。
- builder が出す `shushi_*` dynamic keys がすべて pack に存在するか exporter 側で処理される。

### 完了条件
- builder 生成キーが 1 件でも pack 未定義なら CI が fail する。
- white 由来の回帰を pack lint が検出できる。

---

## P1-04 `TaxYearDefinitionLoader` の存在確認テストを帳票別に拡張する

### 内容
- `common / blue_general / white_shushi` だけでなく、`blue_cash_basis` も必須対象に入れる。
- `validateCoverage` を `TaxLine` だけでなく、pack と builder の差分検査へ拡張する。

### 対象ファイル
- `ProjectProfit/Services/TaxYearDefinitionLoader.swift`
- `ProjectProfitTests/TaxYearDefinitionLoaderTests.swift`

### 根拠
- current existence test は `blue_cash_basis` を見ていない。
- current `validateCoverage` は `TaxLine.rawValue` ベースで、common / detail / dynamic key の不足を検知できない。

### 追加する white 詳細観点
- `white_shushi` で page 2 / requiredRule / dynamic key の不足も検知対象へ含める。

### 完了条件
- `blue_cash_basis` を含む帳票存在テストが追加される。
- pack coverage の穴が loader test で検知できる。

---

## P1-05 release quality 証跡を current HEAD と同期する

### 内容
- `Docs/release/quality/latest.md` の `head_sha` と actual checkout HEAD の不一致を解消する。
- `golden-baseline / canonical-e2e / migration-rehearsal / performance-gate` の error 状態を current HEAD で解消するか、release 不可を明記する。

### 対象ファイル
- `Docs/release/quality/latest.md`
- `Docs/release/quality/golden-baseline.md`
- `Docs/release/quality/canonical-e2e.md`
- `Docs/release/quality/migration-rehearsal.md`
- `Docs/release/quality/performance-gate.md`
- `Docs/release/checklist.md`

### 根拠
- quality 個票が current HEAD と同期していない。
- current 個票は複数 lane が `error`。

### 完了条件
- current HEAD の quality 状態が 1 セットに揃う。
- release 判断に使う SHA が一意に定まる。

---

## P1-06 書類作成対象の範囲を README / release doc に固定する

### 内容
- 今回のスコープが「必要書類の作成まで」であることを明文化する。
- 未対象の機能（直接提出・署名・送受信）は scope 外として明記する。
- 作成対象書類（青色一般 / 白色 / 現金主義）と、未対応 page / detail が無い状態をリリース条件にする。

### 対象ファイル
- `README.md`（あれば）
- `Docs/release/checklist.md`
- `Docs/release/...`

### 根拠
- 現在のやり取りでは提出機能を除外する要件が明確。
- その一方で書類作成の範囲は docs 上で固定されていない。
- white 詳細 Markdown でも white 側 scope 固定が必要とされている。

### 完了条件
- release docs だけを読んで「何を作れるか / 何を作れないか」が分かる。
- scope 外の未実装を defect と混同しない状態になる。

---

# 5. 実行順（推奨）

## 5-1. 全体の推奨順
1. **P0-01** 期限修正  
2. **P0-02 〜 P0-04** 現金主義の帳票系統と export 経路修正  
3. **P0-05 / P0-06 / P0-07 / P0-09** XML 構造・共通タグ・複合要素対応  
4. **P0-08 / P0-10 / P0-11** 個別帳票マッピングと詳細明細の拡張  
5. **P0-12** official XSD 自動検証を CI へ固定  
6. **P1-01 〜 P1-06** 年分別検証、stale 防止、coverage 監査、release 文書整備

## 5-2. 白色申告の推奨順（詳細）
1. **P0-01** deadline 修正  
2. **P0-06** `KOA110-1 / KOA110-2` 2ページ構造へ分割  
3. **P0-10-a** `AIG00020` child 化  
4. **P0-10-b** 経費ブロック parent path 修正  
5. **P0-10-c** 欠落経費項目追加  
6. **P0-10-d** `AIN` 内訳の作り直し  
7. **P0-10-e** `AIK / AIL / AIM` 追加  
8. **P0-10-f** `requiredRule` 付与  
9. **P0-07 / P0-10-g** declarant / 年分タグ修正  
10. **P0-12 / P0-10-h** 実生成 XML の XSD 検証へ移行  
11. **P1-01〜P1-06** 差分チェック / lint / preview-export parity / scope 文書

---

# 6. 元 Markdown からの対応関係

## 6-1. `修正タスク一覧_P0_P1_必要書類作成のみ.md` 由来
- P0-01 〜 P0-12
- P1-01 〜 P1-06
- 全体スコープ / スコープ外 / 最低限の受け入れ基準

## 6-2. `白色申告_詳細修正タスク一覧_P0_P1.md` 由来
- 白色確認事項（現状 / official current / 不整合）
- `P0-W01` 〜 `P0-W10`
- `P1-W01` 〜 `P1-W05`
- white 実装修正の推奨順序
- white 完了判定

## 6-3. 統合時のマージルール
- `P0-W01` は `P0-01` に統合
- `P0-W02` は `P0-06` に統合
- `P0-W03` 〜 `P0-W10` は `P0-10` と `P0-12`、一部 `P0-07` / `P1-*` に内包
- `P1-W01` 〜 `P1-W05` は `P1-01` 〜 `P1-06` に内包
- white 詳細 Markdown にある **手戻り防止メモ** と **white の詳細作業** は、本統合版では各対象タスクの内部タスクとして保持した

---

# 7. 最低限の受け入れ基準（今回スコープ）

以下を満たした時点を「必要書類の作成まで」の完了条件とする。

- 青色一般・白色・現金主義の 3 種類すべてで、current exporter 実出力が official current XSD に通る。
- 2025年分の申告期限が official 公表日と一致する。
- pack / builder / exporter / tests が現金主義を含めて整合する。
- 青色の貸借対照表詳細、白色の必要な内訳・付表が export payload から欠落しない。
- release quality 証跡が current HEAD と一致する。

## 7-1. 白色側の完了判定（詳細）
以下をすべて満たしたら、白色申告（収支内訳書）まわりの修正完了とする。

- `white_shushi.json` 2025/2026 が official current 帳票と整合する。
- `KOA110-1` / `KOA110-2` の 2 ページ構造で XML が出る。
- `AIG00020` / `AIN00090` の誤用が無い。
- `AIK / AIL / AIM / AIN` を含む page 2 項目が存在する。
- direct value を持つ white field が official leaf tag のみになる。
- white generated XML が bundled official XSD に通る。
- white の XSD 検証が fixture fallback に依存しない。
- 2025 / 2026 の white pack 差分が year / deadline 以外で一致する。

